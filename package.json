{
  "name": "email-warmup-service",
  "version": "1.0.0",
  "description": "Email warm-up API service built on Cloudflare Workers",
  "main": "src/api/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute email-warmup-db --file=./schema.sql"
  },
  "keywords": [
    "email",
    "warmup",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260115.0",
    "typescript": "^5.9.3",
    "wrangler": "^3.85.0"
  },
  "dependencies": {
    "hono": "^4.0.0"
  }
}
